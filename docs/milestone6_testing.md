# Milestone 6 Testing

## Logged Request Example
A GET request to /api/users logs the method, route, and timestamp in the server console.

## Handled Error Example
When a database error occurs, the API returns a JSON response with an error message instead of crashing.

## Validation Error Example
A POST request to /api/reservations without required fields returns a 400 error with a clear message.
